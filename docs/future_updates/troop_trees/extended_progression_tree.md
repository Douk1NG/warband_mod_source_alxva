# Extended Unit Progression and Branching Tree

This document outlines the hierarchical branching structure and progression path, extending further through quality tiers up to four stars (`****`).

*Note: `*`, `**`, `***`, and `****` represent unit quality tiers.*

---

## Structure Overview

```
Branch (A)
 ├── B.1
 │    ├── C.1 (*) -> D1 (**) -> E1 (***) -> F1 (****)
 │    └── C.2 (*) -> D2 (**) -> E2 (***)
 │
 └── B.2
      ├── C.3 (*)
      │    ├── D3 (**) -> E3 (***) -> F2 (****)
      │    └── D4 (**) -> E4 (***)
      │
      └── C.4 (*)
           ├── D5 (**) -> E5 (***)
           └── D6 (**) -> E6 (***) -> F3 (****)
```

---

## Detailed Breakdown by Stage

### 1. Initial State
* **Branch A**

### 2. Primary Specialization Branches (Stage B)
* **B.1 Path:**
  * **C.1 (*):** Leads to `D1 (**)` -> `E1 (***)` -> `F1 (****)`
  * **C.2 (*):** Leads to `D2 (**)` -> `E2 (***)`
* **B.2 Path:**
  * **C.3 (*):** Splits into:
    * `D3 (**)` -> `E3 (***)` -> `F2 (****)`
    * `D4 (**)` -> `E4 (***)`
  * **C.4 (*):** Splits into:
    * `D5 (**)` -> `E5 (***)`
    * `D6 (**)` -> `E6 (***)` -> `F3 (****)`
